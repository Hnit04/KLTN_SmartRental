// Re-export from FavoritesContext for backward compatibility
// All components importing from '@/hooks/useFavorites' will now use the shared context
export { useFavorites } from '@/context/FavoritesContext';
