package iuh.se.kltn.backend.modules.contract.enums;

public enum ContractStatus {
    PENDING_APPROVAL,
    PENDING_SIGNATURE,
    AWAITING_DEPOSIT,
    ACTIVE,
    EXPIRED,
    TERMINATED_EARLY,
    CANCELLED
}