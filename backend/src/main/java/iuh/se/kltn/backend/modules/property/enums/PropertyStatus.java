package iuh.se.kltn.backend.modules.property.enums;

public enum PropertyStatus {
    PENDING,    // Đang chờ duyệt
    APPROVED,   // Đã duyệt (Hiển thị công khai)
    REJECTED,    // Bị từ chối
    HIDDEN       // Tạm ẩn
}
