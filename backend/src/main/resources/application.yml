server:
  port: 8080

spring:
  application:
    name: smart-rental
  datasource:
    # URL kết nối MariaDB, lưu ý port 3307

    url: ${DB_URL:jdbc:mariadb://localhost:3306/smart_rental_db?useSSL=false&serverTimezone=UTC&useUnicode=true&characterEncoding=utf8mb4}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}

    # Driver chuẩn của MariaDB
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      # Tự động cập nhật schema DB khi có thay đổi Entity
      ddl-auto: update
    show-sql: true # Hiển thị câu lệnh SQL trong console
    properties:
      hibernate:
        # Dialect tối ưu cho MariaDB
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true # Format SQL cho dễ đọc

  # Cấu hình upload file (nếu cần dùng sau này)
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# --- CẤU HÌNH TÙY CHỈNH ---
# JWT Config (Bắt buộc phải có để chạy JwtTokenProvider)
jwt:
  secret: "${JWT_SECRET:7u7KqQ4TQXtK5vXvKDGZ9h3jHfYZ8vWqLxP9xJv0C7z7Fj8xE1JvY7TqHkQzU2JmO2XxZs7NfUqR2BvV7hTzN2RwXqQwWmR8}"
  expiration: 86400000        # 1 ngày
  refreshExpiration: 2592000000 # 30 ngày
cloudinary:
  cloud-name: "${CLOUDINARY_CLOUD_NAME:dvvlxnwuk}"
  api-key: "${CLOUDINARY_API_KEY:553714881642637}"
  api-secret: "${CLOUDINARY_API_SECRET:sAL6KmfpT3N9kY0OePv6uVnt6Sg}"  
# File Storage
file:
  upload-dir: "uploads/"
fpt:
  ai:
    api-key: "${FPT_AI_KEY:7QwK7PAR4s6zTLfoG3lUbiK9HYNNgsMo}"
    url: "https://api.fpt.ai/vision/idr/vnm"
blockchain:
  # Cổng kết nối vào mạng Sepolia (Public RPC)
#  rpc-url: "https://rpc.sepolia.org"
  rpc-url: "https://ethereum-sepolia-rpc.publicnode.com"
  # Private Key ví Admin/Chủ hệ thống (Ví sẽ trả phí Gas để deploy hợp đồng)
  # ⚠️ Thay dòng dưới bằng Private Key ví MetaMask của bạn (Không phải 12 từ khóa)
  private-key: "${BLOCKCHAIN_PRIVATE_KEY:130a2f782b80a9d736b8e667e0bbcbc235f12fcf93f81173be697e7f7995520d}"

  # Chain ID của Sepolia
  chain-id: 11155111
gemini:
  api:
    key: "${GEMINI_API_KEY:AIzaSyAxR0Wp6zXBS8W8ZpByhpoS5-WY3F3zONg}"
jpa:
  hibernate:
    ddl-auto: update
  show-sql: true
  properties:
    hibernate:
      dialect: org.hibernate.dialect.MariaDBDialect
      format_sql: true
      # --- CẤU HÌNH HIBERNATE ENVERS (THÊM VÀO ĐÂY) ---
      envers:
        # Hậu tố cho bảng audit (VD: users_aud)
        audit_table_suffix: _aud
        # Tên cột lưu ID phiên bản
        revision_field_name: rev
        # Tên cột lưu loại thay đổi (0: ADD, 1: MOD, 2: DEL)
        revision_type_field_name: revtype
        # Luôn lưu dữ liệu kể cả khi xóa
        store_data_at_delete: true
        # Bắt buộc ghi log ngay cả khi các trường không thay đổi giá trị (giúp audit chặt chẽ hơn)
        audit_strategy: org.hibernate.envers.strategy.DefaultAuditStrategy
brevo:
  api:
    key: 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
  sender:
    email: tranngochung19112004@gmail.com
    name: SmartRental
google:
  client-id: 853040464300-f2l17df4g8vf714ainbi1n4a74hk87g2.apps.googleusercontent.com
